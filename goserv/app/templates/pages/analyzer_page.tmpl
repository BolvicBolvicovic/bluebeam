<!DOCTYPE html>
<html>
  <head>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100">
    {{template "navbar" .Navbar}}
    <br/>
    <div class="flex flex-col items-center mx-auto p-6 bg-white shadow-lg rounded-lg max-w-4xl">
      <title>Analyzer</title>
      <form id="urls">
        <textarea placeholder="write one root url per line"></textarea>
        {{template "button" .UrlsSubmitButton}}
      </form>
    </div>
    <br/>
  </body>
</html>
